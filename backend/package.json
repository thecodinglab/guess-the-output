{
  "name": "backend",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "serve": "nodemon --watch 'src/**/*.ts' --exec ts-node src/index.ts",
    "build": "tsc -p . --outDir dist/"
  },
  "dependencies": {
    "socket.io": "^4.1.3"
  },
  "devDependencies": {
    "@types/node": "^16.6.1",
    "nodemon": "^2.0.12",
    "ts-node": "^10.2.0",
    "typescript": "^4.3.5"
  }
}
